import { getInput, getInput2, getTestInput, getTestInput2 } from "./input";
import "../util";
import { iterator } from "../util";

console.log(
	getTestInput()
		.split("\n")
		.map(([ins, _, ...maps]) => [])
);
